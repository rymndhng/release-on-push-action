name: Tests
on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: rymndhng/tag-on-push-action@master
        env:
          DRYRUN: 1
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
